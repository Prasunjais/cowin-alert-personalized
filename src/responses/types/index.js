module.exports = {
  alert: require('./alert'), // including the user authentication message
  joiValidation: require('./joiValidation'), // joi validation error
  states: require('./states'), // states 
  cities: require('./cities'), // cities
};
