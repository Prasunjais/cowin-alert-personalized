// user controller 
const ctrl = require('./alerts.controller');

// custom hooks 
const {
} = require('../../hooks');

// exporting the user routes 
function userRoutes() {
  return (open, closed) => {
  };
}

module.exports = userRoutes();
